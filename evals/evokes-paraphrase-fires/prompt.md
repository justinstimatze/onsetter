---
name: evokes ask fires on a paraphrase
tags: [ask, evokes, core]
allowed_tools: [Edit]
---

`docs/perf-notes.md` already exists with one line in it. Add a new line at
the end saying that the new caching layer roughly doubles throughput on the
hot path. Don't add a heading — just the one sentence, appended after the
existing line.
