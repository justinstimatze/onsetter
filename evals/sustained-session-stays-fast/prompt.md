---
name: sustained session — many sequential edits all land
tags: [performance, transport]
allowed_tools: [Write]
max_turns: 20
runs: 3
---

Create eight small Python modules under `src/`: `mod_1.py` through
`mod_8.py`. Each file should define one function named after its module
(`mod_1` defines `mod_1()`, and so on) that just returns the module's
number. Write them one at a time, in eight separate edits.
