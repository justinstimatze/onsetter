---
name: block ask denies the first attempt and forces a retry
tags: [ask, block, added, core]
allowed_tools: [Edit]
max_turns: 8
---

Add a `DEBUG = True` flag near the top of `src/config.py`, for local
development convenience.
