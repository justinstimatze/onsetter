---
name: ask stays silent on an unrelated Write
tags: [ask, when, core, negative]
allowed_tools: [Write]
---

Create `notes.txt` at the repo root with a short line reminding whoever
reads it to update the changelog before tagging the next release.
